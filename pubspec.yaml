name: todo_app
description: "A new Flutter project."
publish_to: "none" 

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  uuid: ^4.5.0
  intl: ^0.19.0
  flutter_datetime_picker: ^1.5.1
  supabase_flutter: ^2.6.0
  provider: ^6.1.2
  flutter_colorpicker: ^1.0.3  # Biblioteca para o seletor de cor

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
